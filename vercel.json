{
  "version": 2,
  "builds": [{ "src": "server/server.js", "use": "@vercel/node-bridge" }],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/server.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
